import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 0.6,
    justifyContent: 'center',
  },
  text: {
    textAlign: 'center',
    fontSize: 20,
    fontStyle: 'normal',
    letterSpacing: 0,
  },
});
